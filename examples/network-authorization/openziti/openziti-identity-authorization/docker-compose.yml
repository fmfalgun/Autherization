version: '3.8'

services:
  opa:
    image: openpolicyagent/opa:latest
    container_name: openziti-identity-authorization-opa
    ports:
      - "8181:8181"
    command:
      - "run"
      - "--server"
      - "--log-level=info"
    volumes:
      - ./policy.rego:/policies/policy.rego:ro
      - ./data.json:/data/data.json:ro
    networks:
      - openziti-identity-authorization-network

networks:
  openziti-identity-authorization-network:
    driver: bridge
