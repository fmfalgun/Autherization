version: '3.8'

services:
  opa:
    image: openpolicyagent/opa:latest
    container_name: spire-workload-attestation-opa
    ports:
      - "8181:8181"
    command:
      - "run"
      - "--server"
      - "--log-level=info"
    volumes:
      - ./policy.rego:/policies/policy.rego:ro
      - ./data.json:/data/data.json:ro
    networks:
      - spire-workload-attestation-network

networks:
  spire-workload-attestation-network:
    driver: bridge
