version: '3.8'

services:
  opa:
    image: openpolicyagent/opa:latest
    container_name: ble-4.2-opa
    ports:
      - "8181:8181"
    command:
      - "run"
      - "--server"
      - "--log-level=info"
    volumes:
      - ./policy.rego:/policies/policy.rego:ro
      - ./data.json:/data/data.json:ro
    networks:
      - ble-4.2-network

networks:
  ble-4.2-network:
    driver: bridge
