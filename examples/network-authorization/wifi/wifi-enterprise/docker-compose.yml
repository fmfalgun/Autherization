version: '3.8'

services:
  opa:
    image: openpolicyagent/opa:latest
    container_name: wifi-enterprise-opa
    ports:
      - "8181:8181"
    command:
      - "run"
      - "--server"
      - "--log-level=info"
    volumes:
      - ./policy.rego:/policies/policy.rego:ro
      - ./data.json:/data/data.json:ro
    networks:
      - wifi-enterprise-network

networks:
  wifi-enterprise-network:
    driver: bridge
