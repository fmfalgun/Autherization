version: '3.8'

services:
  opa:
    image: openpolicyagent/opa:latest
    container_name: lorawan-1.0.4-opa
    ports:
      - "8181:8181"
    command:
      - "run"
      - "--server"
      - "--log-level=info"
    volumes:
      - ./policy.rego:/policies/policy.rego:ro
      - ./data.json:/data/data.json:ro
    networks:
      - lorawan-1.0.4-network

networks:
  lorawan-1.0.4-network:
    driver: bridge
